/**
 * Layout Components - Barrel Export
 *
 * This module exports layout components
 * used throughout the BankShield Auth application.
 *
 * Future components will include:
 * - AuthLayout (for login/register pages)
 * - DashboardLayout (for authenticated pages with sidebar)
 * - PublicLayout (for landing/marketing pages)
 * - SettingsLayout (for settings pages with sub-navigation)
 */

export {};
