/**
 * Context Providers - Barrel Export
 *
 * This module exports context providers
 * used throughout the BankShield Auth application.
 *
 * Future contexts will include:
 * - AuthProvider: Authentication state management
 * - ThemeProvider: Theme configuration (already provided by next-themes)
 * - SessionProvider: Active session management
 * - SecurityProvider: Security event and risk state
 * - NotificationProvider: In-app notification management
 */

export {};
